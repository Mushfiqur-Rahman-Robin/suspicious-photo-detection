"""io_layer package (flat src layout, ARCHITECTURE 4).

Named io_layer instead of io because the stdlib ``io`` module is always
resident, which makes a top-level package called ``io`` unimportable.
"""
